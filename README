Installation:
    git close git://github.com/stevenmikes/dotvim.git ~/.vim
Create Symbolic Link:
    ln -s ~/.vim/vimrc ~/.vimrc
Switch to the '~/.vim' directory and fetch submodules:
    cd ~/.vim
    git submodule init
    git submodule update
